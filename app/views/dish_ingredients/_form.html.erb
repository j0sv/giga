<%= form_with(model: dish_ingredient, local: true) do |form| %>
  <% if dish_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dish_ingredient.errors.count, "error") %> prohibited this dish_ingredient from being saved:</h2>

      <ul>
      <% dish_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :dish_ingredient_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
